      <div class="container">
        <div class="row">
          <div class="col-lg-12">
            <div class="page-header">
              <h1 id="tables">ひとこと一覧</h1>
            </div>

            <div class="bs-component">
              <table class="table table-hover table-striped">
                <thead>
                  <tr>
                    <th scope="col">ひとことID</th>
                    <th scope="col">タイトル</th>
                    <th scope="col">登録者</th>
                    <th scope="col"></th>
                  </tr>
                </thead>

                <tbody>
                  <% @words.each do |word| %>
                  <tr>
                    <td><%= word.id %></td>
                    <td><%= word.title %></td>
                    <td><%= word.title %></td>
                    <td><%= link_to "編集", admins_word_path(word) %></td>
                  </tr>
                  <% end %>
                </tbody>
              </table>

            </div>
          </div>
        </div>
      </div>
